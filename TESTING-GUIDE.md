# 🧪 FROST-T Testing & Verification Guide

> **完整的測試與驗證指南，確保 Demo 萬無一失**

---

## 📚 目錄

1. [概述](#概述)
2. [測試工具](#測試工具)
3. [快速開始](#快速開始)
4. [測試流程](#測試流程)
5. [API 端點說明](#api-端點說明)
6. [故障排除](#故障排除)

---

## 概述

FROST-T 提供了完整的測試和驗證工具鏈，包括：

- **自動化測試腳本** - Python 腳本全面驗證所有功能
- **快速測試腳本** - 一鍵檢查環境和基本功能
- **手動驗證清單** - 詳細的 checklist 確保展示品質
- **API 端點** - 用於監控和控制的 HTTP API

---

## 測試工具

### 📁 檔案清單

```
frost-threshold-signature/
├── demo-basic.bat              # 一鍵啟動完整 demo
├── quick-test.bat              # 快速測試 (Windows)
├── quick-test.sh               # 快速測試 (Linux/Mac)
├── verify_demo.py              # 完整自動化測試
├── VERIFICATION-CHECKLIST.md   # 手動驗證清單
├── TESTING-GUIDE.md            # 本檔案
└── dashboard.html              # 視覺化 Dashboard
```

### 🔧 工具功能對比

| 工具 | 用途 | 執行時間 | 適用場景 |
|------|------|---------|---------|
| `quick-test.bat/sh` | 快速環境檢查 | ~10 秒 | 展示前快速確認 |
| `verify_demo.py` | 完整功能測試 | ~30 秒 | 深度驗證所有功能 |
| `VERIFICATION-CHECKLIST.md` | 手動檢查清單 | 5-10 分鐘 | 展示演練和準備 |
| `demo-basic.bat` | 完整 Demo 流程 | ~20 秒 + Server 持續運行 | 展示和測試 |

---

## 快速開始

### 選項 1: 一鍵測試（推薦）

**Windows:**
```bash
quick-test.bat
```

**Linux/Mac:**
```bash
chmod +x quick-test.sh
./quick-test.sh
```

這將自動檢查：
- ✓ Rust 環境
- ✓ 編譯狀態
- ✓ Port 可用性
- ✓ API 端點
- ✓ Dashboard 檔案

### 選項 2: 完整自動化測試

**前置條件**: 先啟動 Server
```bash
# Terminal 1
demo-basic.bat

# Terminal 2
python verify_demo.py
```

或使用 Python 3:
```bash
python3 verify_demo.py
```

**需要的 Python 套件:**
```bash
pip install requests
```

### 選項 3: 手動驗證

查看詳細的檢查清單：
```bash
# Windows
type VERIFICATION-CHECKLIST.md

# Linux/Mac
cat VERIFICATION-CHECKLIST.md
```

---

## 測試流程

### 📊 建議的測試順序

#### 展示前 1 天

1. **完整測試**
   ```bash
   # Step 1: 執行 demo
   demo-basic.bat

   # Step 2: 在另一個終端執行完整測試
   python verify_demo.py
   ```

2. **手動驗證所有項目**
   - 按照 `VERIFICATION-CHECKLIST.md` 逐項檢查
   - 確認 Dashboard 視覺效果
   - 觀察至少 1 次掉包重傳

3. **演練展示腳本**
   - 30 秒快速版
   - 2 分鐘完整版
   - 準備技術問答

#### 展示前 1 小時

1. **快速測試**
   ```bash
   quick-test.bat
   ```

2. **確認關鍵點**
   - [ ] CLI 輸出正常
   - [ ] Dashboard 連線成功
   - [ ] API 回應正常

#### 展示前 5 分鐘

1. **最後確認**
   ```bash
   # 確認編譯
   cargo check --bin frost-cli

   # 確認 Dashboard 可開啟
   start dashboard.html
   ```

2. **準備備用方案**
   - 截圖/錄影
   - 備用電腦
   - 離線文件

---

## API 端點說明

FROST-T HTTP Server 提供以下 API 端點：

### GET /health

**用途**: 健康檢查，確認 Server 運行

**範例:**
```bash
curl http://127.0.0.1:3000/health
```

**回應:**
```json
{
  "status": "ok",
  "service": "frost-threshold-signature",
  "version": "0.1.0"
}
```

**狀態碼:**
- `200 OK` - Server 正常

---

### GET /status

**用途**: 取得當前 LoRa 傳輸狀態

**範例:**
```bash
curl http://127.0.0.1:3000/status
```

**回應:**
```json
{
  "current_phase": "Round1",
  "total_messages": 5,
  "total_bytes": 1234,
  "progress": 0.6,
  "rssi": -75,
  "recent_events": [
    {
      "type": "TransmitStart",
      "from": "signer_1",
      "to": "coordinator",
      "message_type": "Round1Commitment",
      "total_bytes": 132,
      "fragments": 3
    },
    {
      "type": "TransmitFragment",
      "fragment_id": 1,
      "total_fragments": 3,
      "bytes": 64
    },
    {
      "type": "PacketLost",
      "fragment_id": 2,
      "retry_count": 1
    }
  ],
  "by_type": {
    "Round1Commitment": 3,
    "Round2SignatureShare": 3
  },
  "total_retries": 2
}
```

**欄位說明:**

| 欄位 | 類型 | 說明 |
|------|------|------|
| `current_phase` | String | 當前階段 (KeyGeneration, Round1, Round2, etc.) |
| `total_messages` | Number | 總訊息數 |
| `total_bytes` | Number | 總傳輸位元組數 |
| `progress` | Number | 進度 (0.0 到 1.0) |
| `rssi` | Number | 虛擬訊號強度 (-120 到 -30 dBm) |
| `recent_events` | Array | 最近的傳輸事件 |
| `by_type` | Object | 按類型統計的訊息數 |
| `total_retries` | Number | 總重傳次數 |

**事件類型:**

- `TransmitStart` - 傳輸開始
- `TransmitFragment` - 片段傳輸
- `PacketLost` - 掉包
- `RetrySuccess` - 重傳成功
- `TransmitComplete` - 傳輸完成

---

### POST /sign

**用途**: 觸發簽章流程

**範例:**
```bash
curl -X POST http://127.0.0.1:3000/sign \
  -H "Content-Type: application/json" \
  -d '{
    "message": "bitcoin++ Taipei 2025",
    "signer_ids": [1, 2, 3]
  }'
```

**請求 Body:**
```json
{
  "message": "要簽章的訊息",
  "signer_ids": [1, 2, 3]
}
```

**回應:**
```json
{
  "signature": "a1b2c3d4e5f6...",
  "verified": true,
  "message": "bitcoin++ Taipei 2025",
  "signer_ids": [1, 2, 3]
}
```

**欄位說明:**

| 欄位 | 類型 | 說明 |
|------|------|------|
| `signature` | String | Schnorr 簽章 (hex 格式) |
| `verified` | Boolean | 簽章是否驗證通過 |
| `message` | String | 原始訊息 |
| `signer_ids` | Array | 參與簽章的簽署者 ID |

**注意事項:**
- 簽署者 ID 必須在 1-5 之間
- 至少需要 3 個簽署者（門檻值）
- 簽章過程會更新 `/status` 端點的狀態

---

## verify_demo.py 詳細說明

### 執行步驟

`verify_demo.py` 會依序執行以下測試：

#### 1️⃣ 健康檢查
- 呼叫 `GET /health`
- 確認 Server 回應 200 OK
- 顯示服務資訊

#### 2️⃣ 初始狀態監控
- 持續呼叫 `GET /status` (3 秒)
- 每 0.5 秒更新一次
- 顯示即時狀態

#### 3️⃣ 發送簽章請求
- 記錄開始前的 `total_bytes` 和 `progress`
- 發送 `POST /sign` 請求
- 記錄完成後的狀態
- 驗證變化：
  - `total_bytes` 是否增加
  - `progress` 是否達到 1.0

#### 4️⃣ 監控簽章過程
- 持續監控 5 秒
- 觀察 `progress` 從 0.0 → 1.0
- 觀察 `current_phase` 變化
- 記錄最大 `total_retries`

#### 5️⃣ 驗證簽章回應
- 檢查 `signature` 欄位存在且有效
- 檢查 `verified` 為 `true`
- 檢查 `message` 欄位
- 檢查 `signer_ids` 欄位

#### 6️⃣ 檢查事件日誌
- 讀取 `recent_events`
- 顯示最近 10 個事件
- 驗證事件類型和格式

### 預期輸出

**成功案例:**
```
╔════════════════════════════════════════════════════════════════════╗
║              FROST-T Demo 自動化驗證腳本                          ║
╚════════════════════════════════════════════════════════════════════╝

========= 步驟 1: 健康檢查 =========
✓ Server 正常運行
ℹ 服務: frost-threshold-signature
ℹ 版本: 0.1.0

========= 步驟 2: 初始狀態監控 =========
[10:30:45] Phase: Complete        | Progress:  100.0% | Messages:  10 | ...
...

========= 步驟 3: 發送簽章請求 =========
✓ 簽章請求成功！
✓ total_bytes 有增加 (1234 → 2468)
✓ progress 達到 1.0 (1.00)

========= 驗證摘要 =========
✓✓✓ 所有驗證項目通過！ ✓✓✓
```

**失敗案例:**
```
✗ 無法連線到 server！
請確認:
  1. 已執行 demo-basic.bat 或手動啟動 server
  2. Server 正在監聽 http://127.0.0.1:3000
```

---

## quick-test 腳本說明

### quick-test.bat (Windows)

**執行步驟:**
1. 檢查 Rust 環境 (`cargo --version`)
2. 檢查編譯狀態 (`cargo check`)
3. 檢查 Port 3000 是否可用
4. 如果 port 可用，啟動 Server
5. 測試 `/health` 端點
6. 測試 `/status` 端點
7. 檢查 `dashboard.html` 存在
8. 自動開啟 Dashboard

**適用場景:**
- 展示前快速確認
- 開發環境初次設定
- 故障排除

### quick-test.sh (Linux/Mac)

功能與 Windows 版本相同，但使用 bash 語法。

**執行前需要:**
```bash
chmod +x quick-test.sh
```

**特色:**
- 支援 macOS 和 Linux
- 彩色終端輸出
- 自動偵測並開啟瀏覽器

---

## 故障排除

### 問題 1: 無法連線到 Server

**症狀:**
```
✗ 無法連線到 /health
```

**解決方法:**
1. 確認 Server 正在運行:
   ```bash
   # Windows
   netstat -ano | findstr :3000

   # Linux/Mac
   lsof -i :3000
   ```

2. 如果沒有運行，啟動 Server:
   ```bash
   demo-basic.bat
   ```

3. 檢查防火牆設定

---

### 問題 2: Python 找不到 requests 模組

**症狀:**
```
ModuleNotFoundError: No module named 'requests'
```

**解決方法:**
```bash
pip install requests

# 或使用 pip3
pip3 install requests
```

---

### 問題 3: Port 3000 被佔用

**症狀:**
```
✗ Port 被其他程式佔用
```

**解決方法:**

**Windows:**
```bash
# 找出佔用的 process
netstat -ano | findstr :3000

# 假設 PID 是 12345
taskkill /PID 12345 /F
```

**Linux/Mac:**
```bash
# 找出並結束 process
lsof -ti :3000 | xargs kill -9
```

---

### 問題 4: Dashboard 無法連線

**症狀:**
Dashboard 右上角顯示紅色 `● DISCONNECTED`

**解決方法:**
1. 確認 HTTP Server 正在運行
2. 開啟瀏覽器 DevTools (F12)
3. 檢查 Console 是否有 CORS 錯誤
4. 確認 `http://127.0.0.1:3000/status` 可以在瀏覽器中直接存取

---

### 問題 5: 編譯錯誤

**症狀:**
```
✗ 編譯失敗，請檢查錯誤訊息
```

**解決方法:**
1. 清除編譯快取:
   ```bash
   cargo clean
   ```

2. 更新 Rust:
   ```bash
   rustup update
   ```

3. 重新編譯:
   ```bash
   cargo build --release --bin frost-cli
   ```

4. 檢查相依套件:
   ```bash
   cargo check
   ```

---

### 問題 6: 從未看到掉包重傳

**症狀:**
多次執行 demo，`total_retries` 始終為 0

**解釋:**
這是正常現象。10% 的掉包率是機率性的，不保證每次都發生。

**解決方法（如需確保看到）:**
暫時修改 `src/transport/simulated_lora.rs`:
```rust
pub const DEFAULT_PACKET_LOSS_RATE: f64 = 0.3;  // 改為 30%
```

然後重新編譯:
```bash
cargo build --bin frost-cli
```

**記得展示後改回 0.1！**

---

## 進階測試

### 壓力測試

連續執行多次簽章:
```bash
for i in {1..10}; do
  curl -X POST http://127.0.0.1:3000/sign \
    -H "Content-Type: application/json" \
    -d "{\"message\": \"Test $i\", \"signer_ids\": [1, 2, 3]}"
  echo ""
done
```

### 並發測試

使用 Apache Bench:
```bash
ab -n 10 -c 2 http://127.0.0.1:3000/health
```

### 長時間穩定性測試

讓 Server 持續運行 1 小時:
```bash
demo-basic.bat
# Server 會持續運行直到按 Ctrl+C
```

定期查詢狀態:
```bash
while true; do
  curl -s http://127.0.0.1:3000/status | jq '.total_messages, .progress'
  sleep 10
done
```

---

## 測試指標

### 性能指標

- **健康檢查回應時間**: < 50ms
- **狀態查詢回應時間**: < 100ms
- **簽章完成時間**: 2-5 秒（取決於重傳次數）
- **Server 記憶體使用**: < 50MB

### 可靠性指標

- **API 可用性**: 99.9%
- **簽章驗證成功率**: 100%
- **掉包重傳成功率**: 95%+（3 次重傳內）

---

## 總結

完整的測試流程應包括：

1. **開發階段**: 使用 `verify_demo.py` 完整測試
2. **展示準備**: 使用 `VERIFICATION-CHECKLIST.md` 逐項確認
3. **展示前**: 使用 `quick-test.bat/sh` 快速驗證
4. **展示中**: 使用 `demo-basic.bat` 進行展示

**記住**: 測試是確保展示成功的關鍵！

---

## 相關文件

- [README.md](README.md) - 專案主頁
- [PHASE2-README.md](PHASE2-README.md) - Phase 2 完整文件
- [VERIFICATION-CHECKLIST.md](VERIFICATION-CHECKLIST.md) - 手動驗證清單
- [CLI-README.md](CLI-README.md) - CLI 使用說明

---

**祝測試順利！🚀**

# 🚀 FROST-T Hackathon 展示指南

> Bitcoin++ Taipei 2025 黑客松展示完整配置指南

## 📋 目錄

- [展示架構](#展示架構)
- [前置準備](#前置準備)
- [方案 A：雙機展示（推薦）](#方案-a雙機展示推薦)
- [方案 B：單機展示](#方案-b單機展示)
- [方案 C：完全自主展示（Surface Go 4 獨立運行）](#方案-c完全自主展示)
- [故障排除](#故障排除)

---

## 🏗️ 展示架構

### 方案 A：雙機展示（推薦）⭐

**Mac mini（後端服務器） + Surface Go 4（展示層）**

```
┌─────────────────────────┐         ┌──────────────────────────┐
│   Mac mini              │◄────────┤   Surface Go 4           │
│   (強大的運算後端)       │  WiFi   │   (輕量展示前端)         │
├─────────────────────────┤         ├──────────────────────────┤
│ • API Server :3000      │         │ • SSH Terminal           │
│ • Dashboard Server:8000 │         │ • Browser Dashboard      │
│ • FROST 密碼學運算      │         │ • 投影/簡報輸出          │
│ • 完整 Rust 工具鏈      │         │ • 演講者操作介面         │
└─────────────────────────┘         └──────────────────────────┘
```

**優點**：
- ✅ Mac mini 強大運算能力處理密碼學
- ✅ Surface Go 4 輕量展示，流暢度佳
- ✅ 可展示分散式架構特性
- ✅ 網路斷線時仍可展示離線簽章功能
- ✅ 雙螢幕展示效果專業

**適合場景**：正式黑客松展示、技術演講

---

## 🔧 前置準備

### 1. 網路配置

確保兩台設備在同一網路：

```bash
# Mac mini 查看 IP
ifconfig | grep "inet " | grep -v 127.0.0.1

# 預期輸出範例：
# inet 192.168.1.100 netmask 0xffffff00 broadcast 192.168.1.255
```

記下 Mac mini 的 IP 地址（例如：`192.168.1.100`）

### 2. SSH 設定（方案 A 必要）

#### 在 Mac mini 上啟用 SSH：

1. 系統偏好設定 → 共享
2. 勾選「遠端登入」
3. 允許「所有使用者」或特定使用者

#### 在 Surface Go 4 上測試 SSH：

**Linux / WSL:**
```bash
ssh mac@192.168.1.100
```

**Windows PowerShell:**
```powershell
ssh mac@192.168.1.100
```

#### 設定免密碼登入（可選但推薦）：

**Surface Go 4 (Linux/WSL):**
```bash
ssh-keygen -t rsa -b 4096  # 一路按 Enter
ssh-copy-id mac@192.168.1.100
```

**Surface Go 4 (Windows PowerShell):**
```powershell
# 生成 SSH 金鑰
ssh-keygen -t rsa -b 4096

# 複製公鑰到 Mac mini
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh mac@192.168.1.100 "cat >> .ssh/authorized_keys"
```

### 3. 編譯專案（Mac mini）

```bash
cd ~/Documents/Prj/frost-threshold-signature
cargo build --release
```

> ⏱️ 首次編譯約需 2-5 分鐘

---

## 🎯 方案 A：雙機展示（推薦）

### 步驟 1：在 Mac mini 啟動服務器

**終端 1（或使用一鍵腳本）：**

```bash
cd ~/Documents/Prj/frost-threshold-signature

# 方式 1：一鍵啟動所有服務（推薦）
chmod +x demo-hackathon-all.sh
./demo-hackathon-all.sh

# 方式 2：分別啟動（更靈活）
# 終端 1: API Server
chmod +x demo-hackathon-server.sh
./demo-hackathon-server.sh

# 終端 2: Dashboard Server
chmod +x serve-dashboard.sh
./serve-dashboard.sh
```

**預期輸出：**
```
╔════════════════════════════════════════════════════════════════╗
║   FROST-T Hackathon Demo - Full Stack Mode                    ║
║   Mac mini: API Server + Dashboard Server                     ║
╚════════════════════════════════════════════════════════════════╝

✓ Mac mini IP: 192.168.1.100

📋 Services Configuration:
   ┌────────────────────────────────────────────────┐
   │  API Server:       http://192.168.1.100:3000   │
   │  Dashboard Server: http://192.168.1.100:8000   │
   └────────────────────────────────────────────────┘

✅ All services are running!
```

### 步驟 2：在 Surface Go 4 執行 CLI 展示

**Linux / macOS / WSL:**
```bash
cd /path/to/frost-threshold-signature

# 設定 Mac mini IP（替換為實際 IP）
MAC_IP=192.168.1.100

# 執行展示腳本
chmod +x demo-hackathon-client.sh
./demo-hackathon-client.sh $MAC_IP
```

**Windows (CMD / PowerShell):**
```cmd
REM 設定 Mac mini IP（替換為實際 IP）
demo-hackathon-client.bat 192.168.1.100
```

**手動執行 CLI（更靈活）：**
```bash
# 透過 SSH 執行任意 CLI 命令
ssh mac@192.168.1.100 "cd ~/Documents/Prj/frost-threshold-signature && \
  cargo run --bin frost-cli -- demo-basic -m 'Bitcoin++ Taipei 2025'"

# 自訂簽署者
ssh mac@192.168.1.100 "cd ~/Documents/Prj/frost-threshold-signature && \
  cargo run --bin frost-cli -- demo-basic --signers 1,3,4"

# 完整 payload 模式
ssh mac@192.168.1.100 "cd ~/Documents/Prj/frost-threshold-signature && \
  cargo run --bin frost-cli -- demo-basic --full-payload"
```

### 步驟 3：開啟 Dashboard（Surface Go 4 瀏覽器）

**手動開啟：**
```
http://192.168.1.100:8000/dashboard.html?api=http://192.168.1.100:3000
```

**使用腳本自動開啟（Windows）：**
腳本會在 demo 完成後自動詢問是否開啟瀏覽器。

### 步驟 4：展示流程

1. **介紹架構**（指向兩台機器）
   - Mac mini: 安全的後端運算
   - Surface Go 4: 用戶互動介面

2. **執行 CLI demo**（在 Surface Go 4 終端）
   - 觀眾可以看到 SSH 連線過程
   - 即時顯示 FROST 協議執行

3. **切換到 Dashboard**（投影 Surface Go 4 瀏覽器）
   - 展示 LoRa 模擬傳輸
   - 即時統計數據
   - Cyberpunk 視覺效果

4. **Q&A 互動**
   - 可現場修改簽章訊息
   - 選擇不同簽署者組合
   - 展示簽章驗證

---

## 🖥️ 方案 B：單機展示

**僅使用 Mac mini 展示**

### 步驟 1：啟動服務

```bash
cd ~/Documents/Prj/frost-threshold-signature
./demo-hackathon-all.sh
```

### 步驟 2：開啟終端執行 CLI

**新終端視窗：**
```bash
cd ~/Documents/Prj/frost-threshold-signature
cargo run --bin frost-cli -- demo-basic -m "Hello Bitcoin++"
```

### 步驟 3：開啟 Dashboard

```bash
open http://localhost:8000/dashboard.html
# 或在瀏覽器輸入：http://localhost:8000/dashboard.html
```

**優點**：設定簡單，不需網路
**缺點**：無法展示分散式特性

---

## 💻 方案 C：完全自主展示（Surface Go 4 獨立運行）

**前提**：Surface Go 4 需安裝 Rust 工具鏈

### Windows 安裝 Rust：

1. 下載並安裝 [Rustup](https://rustup.rs/)
2. 安裝 Visual Studio C++ Build Tools
3. 重啟終端

### 執行展示：

```cmd
cd C:\path\to\frost-threshold-signature

REM 編譯專案
cargo build --release

REM 啟動服務器（背景執行）
start /B cargo run --release

REM 等待 3 秒
timeout /t 3

REM 執行 CLI demo
cargo run --bin frost-cli -- demo-basic

REM 開啟 Dashboard
start http://localhost:8000/dashboard.html
```

**優點**：完全獨立，不依賴其他設備
**缺點**：
- Surface Go 4 編譯較慢
- 運行效能可能不如 Mac mini
- 展示準備時間較長

---

## 🎨 展示技巧

### 1. 雙螢幕設定（推薦）

**Surface Go 4 連接投影機：**
- **主螢幕**：瀏覽器 Dashboard（投影給觀眾）
- **副螢幕**：終端 CLI（自己操作）

**快捷鍵（Windows）：**
- `Win + P` → 選擇「延伸」模式

### 2. 終端美化

**安裝 Windows Terminal（推薦）：**
```powershell
winget install Microsoft.WindowsTerminal
```

**配置透明效果：**
1. 開啟 Windows Terminal
2. 設定 → 外觀 → 亞克力材質 → 開啟
3. 不透明度設為 80-90%

### 3. 瀏覽器 F11 全螢幕模式

按 `F11` 隱藏網址列，專注展示 Dashboard

### 4. 預先準備的展示命令

**建立展示腳本 `demo-commands.txt`：**
```bash
# 基本展示
cargo run --bin frost-cli -- demo-basic -m "Bitcoin++ Taipei 2025!"

# 自訂簽署者（展示門檻特性）
cargo run --bin frost-cli -- demo-basic --signers 1,2,3
cargo run --bin frost-cli -- demo-basic --signers 2,4,5

# 完整 payload 模式（展示技術細節）
cargo run --bin frost-cli -- demo-basic --full-payload

# 驗證簽章
cargo run --bin frost-cli -- verify
```

---

## 🐛 故障排除

### 問題 1：Surface Go 4 無法連接 Mac mini

**檢查清單：**
```bash
# 1. Ping 測試
ping 192.168.1.100

# 2. 檢查 SSH 端口
nc -zv 192.168.1.100 22

# 3. 檢查防火牆（Mac mini）
sudo pfctl -sr | grep ssh
```

**解決方案：**
- 確認兩台設備在同一 WiFi
- Mac mini 系統偏好設定 → 共享 → 啟用「遠端登入」
- 關閉 Mac mini 防火牆（系統偏好設定 → 安全性）

### 問題 2：Dashboard 無法載入

**檢查 API 服務器：**
```bash
# 在 Surface Go 4 測試
curl http://192.168.1.100:3000/health

# 預期輸出：
# {"status":"healthy","version":"0.1.0"}
```

**解決方案：**
- 確認 Mac mini 的 `demo-hackathon-all.sh` 正在執行
- 檢查 port 3000 和 8000 是否被其他程式佔用
- 瀏覽器開發者工具（F12）查看 Console 錯誤

### 問題 3：Dashboard 顯示 "DISCONNECTED"

**原因：** API URL 配置錯誤

**解決方案：**
1. 確認 URL 參數正確：
   ```
   http://192.168.1.100:8000/dashboard.html?api=http://192.168.1.100:3000
   ```
2. 開啟瀏覽器 Console（F12），查看 API 連線狀態
3. 測試 API 端點：
   ```bash
   curl http://192.168.1.100:3000/status
   ```

### 問題 4：編譯錯誤

**錯誤訊息：`error: linking with 'cc' failed`**

**解決方案（Mac）：**
```bash
xcode-select --install
```

**解決方案（Windows）：**
安裝 [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022)

### 問題 5：Port 已被佔用

**錯誤訊息：`Address already in use`**

**檢查並終止佔用的程序（Mac）：**
```bash
# 查找佔用 port 3000 的程序
lsof -i :3000

# 終止程序（替換 PID）
kill -9 <PID>
```

**Windows PowerShell:**
```powershell
# 查找佔用的程序
netstat -ano | findstr :3000

# 終止程序
taskkill /PID <PID> /F
```

**更改 Port（臨時方案）：**
```bash
# 使用不同的 Port
HOST=0.0.0.0 PORT=8080 cargo run --release
```

---

## 📊 展示時間規劃

**5 分鐘快速展示：**
1. 開場介紹（30 秒）
2. 展示架構圖（30 秒）
3. 執行 CLI demo（2 分鐘）
4. 展示 Dashboard（1 分鐘）
5. 總結 + Q&A（1 分鐘）

**15 分鐘完整展示：**
1. 開場 + 問題背景（2 分鐘）
2. FROST 協議介紹（3 分鐘）
3. 架構設計講解（2 分鐘）
4. 現場 demo 執行（5 分鐘）
   - 基本流程
   - 自訂參數
   - 展示門檻特性
5. Dashboard 深度展示（2 分鐘）
6. Q&A（1 分鐘）

---

## 🎯 關鍵賣點（展示時強調）

### 技術亮點：
1. ⚡ **3-of-5 門檻簽章** - 任意 3 個簽署者即可完成簽章
2. 🔒 **比特幣相容** - 使用 secp256k1 曲線（Taproot/Schnorr）
3. 📡 **LoRa 模擬** - 模擬低頻寬無線通訊（500ms 延遲、10% 掉包）
4. 🎨 **即時視覺化** - Cyberpunk 風格 Dashboard
5. 🏗️ **模組化架構** - 清晰的 Transport 抽象層

### 應用場景：
- 💰 多重簽章錢包（冷錢包分散）
- 🏢 企業資產管理（分權治理）
- 🌐 去中心化組織（DAO 治理）
- 🛡️ 高安全場景（核武發射、銀行金庫）

### 未來規劃：
- 📲 真實 LoRa 硬體整合
- 📷 QR Code 空隙傳輸
- 📱 NFC 近距通訊
- 🔐 HSM 硬體安全模組

---

## 📝 檢查清單

**展示前一天：**
- [ ] Mac mini 充電 / 供電確認
- [ ] Surface Go 4 充電完成
- [ ] 測試 WiFi 連線（會場環境）
- [ ] 編譯專案（`cargo build --release`）
- [ ] 測試完整展示流程
- [ ] 準備投影轉接頭（HDMI/USB-C）
- [ ] 備份展示腳本和文件

**展示前 30 分鐘：**
- [ ] 連接會場 WiFi（或建立熱點）
- [ ] 記錄 Mac mini IP 地址
- [ ] 啟動 Mac mini 服務器
- [ ] 測試 SSH 連線
- [ ] 測試 Dashboard 訪問
- [ ] 設定投影/雙螢幕
- [ ] 執行一次完整 demo 測試

**展示開始前：**
- [ ] 開啟 Mac mini 服務器（`demo-hackathon-all.sh`）
- [ ] Surface Go 4 瀏覽器開啟 Dashboard
- [ ] 終端準備好 SSH 連線
- [ ] 關閉通知/勿擾模式
- [ ] F11 全螢幕模式（Dashboard）
- [ ] 深呼吸，準備開始！🚀

---

## 🎉 成功展示後

**分享資源：**
- GitHub: https://github.com/yourusername/frost-threshold-signature
- Demo 影片上傳
- 技術文章撰寫
- 社群媒體宣傳

**收集反饋：**
- 觀眾問題記錄
- 改進建議整理
- 潛在合作機會

**持續改進：**
- 根據反饋優化 UI
- 添加新功能
- 準備下一次展示

---

## 📞 支援資源

**文件：**
- [README.md](./README.md) - 專案概述
- [CLI-README.md](./CLI-README.md) - CLI 快速參考
- [CLI-DEMO.md](./CLI-DEMO.md) - CLI 詳細指南

**腳本：**
- `demo-hackathon-all.sh` - Mac mini 一鍵啟動
- `demo-hackathon-client.sh` - Surface Go 4 (Linux/macOS)
- `demo-hackathon-client.bat` - Surface Go 4 (Windows)
- `demo-basic.bat` - 快速測試腳本

---

**祝展示順利！🎉**

*Bitcoin++ Taipei 2025 - FROST-T Team*
